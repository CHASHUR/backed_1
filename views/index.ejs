<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Auto Saved Notepad</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
    <div class="main w-full min-h-screen bg-zinc-900 p-4">
        <div class="form">
            <form action="/create" method="post" class="flex flex-col space-y-4">
                <input 
                    type="text" 
                    placeholder="Title goes here" 
                    name="heading"
                    class="px-4 py-2 border border-gray-300 rounded-md bg-zinc-800 text-white placeholder-gray-400">
                <textarea 
                    name="content"
                    placeholder="Enter your information here" 
                    class="px-4 py-2 border border-gray-300 rounded-md resize-none bg-zinc-800 text-white placeholder-gray-400"></textarea>
                <input 
                    type="submit" 
                    value="Create Task" 
                    class="px-4 py-2 w-1/4 bg-blue-600 text-white rounded-md cursor-pointer hover:bg-blue-700">
            </form>
        </div>

        <div class="tasks py-10 flex gap-3 flex-wrap">
            <% if (files.length > 0) { %>
                <% files.forEach((value) => { %>
                    <div class="task w-72 px-3 py-3 rounded-md bg-zinc-800">
                        <h1 class="text-white text-2xl tracking-wide"><%= value %></h1>
                        <a href="/file/<%= value %>" class="text-blue-500 inline-block mt-3">Read more...</a>
                    </div>
                <% }) %>
            <% } else { %>
                <h3 class="text-zinc-600">No tasks yet...</h3>
            <% } %>
        </div>
    </div>
</body>
</html>
